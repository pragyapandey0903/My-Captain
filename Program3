#include<stdio.h>
 int main() 
 { printf("Enter the number") ;
   scanf("%d", &a) ;
   (a==0||a==1)? (x=1) :(x=2) ;
   
switch(x) 
Case(1) : printf("Number is in the list") ;
break;
Case(2) : printf ("Number is not in the list") ;
break;
}
